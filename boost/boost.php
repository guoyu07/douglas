<?php
/**
 * douglas - Douglas Miller Scholarship
 */

// The full name of your module
$proper_name  = 'Douglas Miller Scholarship';

// The current version of your module TODO: update regularly!
$version      = '1.1.0';

// Does this module register with other modules?
$register     = false;

// Does this module need to unregister?
$unregister   = false;

// Should Boost import an install.sql?
$import_sql   = true;

// TODO: Provide a way to check for module upgrades
// $version_http = 'http:a//phpwebsite.appstate.edu/downloads/modules/???/check.xml';

// Is there an About page?
$about        = false;

// In what order should your module be executed relative to the others?  Lower is higher.
$priority     = 50;

// Is there a dependency.xml to process?
$dependency   = false;

// Do you need to import a directory of images? [deprecated]
$image_dir    = false;

// Do you need to import a directory of files? [deprecated]
$file_dir     = true;

?>
